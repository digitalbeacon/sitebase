<div ng-show="data.alerts && data.alerts.length" class="message-panel ng-hide">
	<alert ng-repeat="alert in data.alerts" type="{{alert.type}}" close="closeAlert($index)"><span ng-bind-html="alert.msg"></span></alert>
</div>

<if condition="Model.Messages.Count > 0">
	<alert type="success" each="var message in Model.Messages">!{M(message)}</alert>
</if>
<else if="Model.Errors.Count > 0">
	<alert type="danger" each="var error in Model.Errors">!{M(error)}</alert>
</else>